// Faça uma função que informe a quantidade de dígitos de um determinado número inteiro informado
let numero = ""
function contarNumero(numeros) {
    numeros = numeros.toString()
    return numeros.length
}

console.log(contarNumero(numero))